---
title: Route Map
---

# Lagos Delivery Network — Route Map

Optimal delivery routes generated by the CVRPTW solver. Each color represents one vehicle. Depot marked in red, customer stops as circles.

<iframe
  src="/hermes/routes_map.html"
  width="100%"
  height="650px"
  style="border:none;border-radius:8px;"
  loading="lazy"
  title="HERMES Route Map">
</iframe>

_Map generated from the latest solver run. Re-deploys automatically on push._

## Scenarios

```sql scenarios
SELECT
    run_id,
    scenario_tag,
    total_cost_km,
    vehicles_used,
    orders_served,
    solver_status,
    created_at
FROM solution_metadata
ORDER BY created_at DESC
```

<DataTable data={scenarios} rows=10 />
